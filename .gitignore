# Python (build-time content tooling only — the app itself is static)
.venv/
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
build/
dist/

# Secrets — never commit the Anthropic key
.env
secrets.json

# Node (only if any JS tooling is added later)
node_modules/

# Editor / OS
.DS_Store
*.log

# Local-only notes (matches sibling caltrain-quick convention)
CLAUDE.md
